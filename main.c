#include <stdio.h>
#include <string.h>
#include "atividade.h"

int main(){
    int *ultima_posicao, opcao;
    int lista_soma[*ultima_posicao];


    if(opcao = 1){
        printf("Digite um numero para que seja realizada a soma: \n");
        scanf(*ultima_posicao);
    }

    else{
        printf("A soma é igual a: %i", &lista_soma);
    }


    return 0;
}